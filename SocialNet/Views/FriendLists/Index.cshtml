@model IEnumerable<SocialNet.ViewModels.FriendList>
@using Microsoft.AspNet.Identity;
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    @*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@
    <table class="table">
        <tr>
            @*<th>
               @User.Identity.Name
            </th>*@
            <th>
                @Html.DisplayName("Friend List")
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.FriendIsTopFriend)
            </th>*@
            <th></th>
        </tr>
        @*<tr>
            <td>
                @Html.DisplayNameFor(model => model.UserName)
            </td>
        </tr>*@
    @*@foreach (var item in Model.Where(i => i.modelItem => item.UserName == this.User.Identity.Name))*@
    @foreach (var item in Model.Where(s => s.UserName == this.User.Identity.Name))
    {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.FriendName)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.FriendIsTopFriend)
            </td>*@
            @*<td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>*@
        </tr>
    }
    
    </table>
</body>
</html>
