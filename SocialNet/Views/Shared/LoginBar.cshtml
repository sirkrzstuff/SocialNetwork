@model SocialNet.ViewModels.ConnectionViewModel
<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("SocialNet!", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        </div>
        <li class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li>@Html.ActionLink("Search", "Search", "FriendLists")</li>
                <li>@Html.ActionLink("Messenger", "Messenger", "Home")</li>
            </ul>
            <ul class="nav navbar-nav">
            </ul>
            @{Html.RenderPartial("_LoginPartial", Model);}
        </div>
        </li>
    </div>
</div>