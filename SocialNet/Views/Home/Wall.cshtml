@using Microsoft.AspNet.Identity;
@model IEnumerable<SocialNet.ViewModels.UserStatus>

@foreach (var u in Model)
{
    <div class="StatusForm">
        <p style="color: grey;">Name = @u.UserName | @u.StatusDate | ID = @u.UserStatusID</p>
        <p>@u.StatusBody</p>
    </div>
}
