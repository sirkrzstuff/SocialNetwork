@using Microsoft.AspNet.Identity;
@model SocialNet.ViewModels.ConnectionViewModel
@{
    ViewBag.Title = "Profile";
}

<body>
    @{Html.RenderPartial("LoginBar", Model);}
    <div class="main-container">
        <div class="Group-photo-container">
            <input type="image" src="@Model.ConnectionGroupsForm.GroupPhoto" class="group-img" alt="The Groups Image" />
        </div>
        <div class="group-horizontal-bar">
            <div style=" padding-left: 15px; width: 35%; text-align: left; display: inline-block;">
                @Html.DisplayFor(itemModel => Model.ConnectionGroupsForm.GroupName)
            </div>
            <div style=" width: 30%; text-align: center; display: inline-block;">
                Members:
            </div>
            <div style=" width: 33%; text-align: right; display: inline-block;">
                @Html.ActionLink("AddMembers", "", "")
            </div>
        </div>
        <div class="wall-posts">
            @Html.Partial("Wall", Model)
        </div>
    </div>
    @Html.Partial("RightBar", Model)
</body>

